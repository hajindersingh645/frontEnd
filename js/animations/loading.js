console.log("reached in here");
lottie.loadAnimation({
    container: document.getElementById("loadingAnimation"),
    path: "./loading.json",
    renderer: "svg",
    loop: true,
    autoplay: true,
    name: "Mailum Animation - Loading",
});
